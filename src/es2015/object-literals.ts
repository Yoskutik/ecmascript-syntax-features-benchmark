import { run } from '../helpers';

run(
  250_000,
  function (i) {
    window.__testValue = {
      ['a' + (i % 5000)]: i,
      ['a' + ((i + 100) % 5000)]: i + 1,
      ['a' + ((i + 200) % 5000)]: i + 2,
      ['a' + ((i + 300) % 5000)]: i + 3,
      ['a' + ((i + 400) % 5000)]: i + 4,
      ['a' + ((i + 500) % 5000)]: i + 5,
    }
  },
);
